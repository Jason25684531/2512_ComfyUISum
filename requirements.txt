# ===========================================
# ComfyUI Studio - 統一依賴管理
# ===========================================
# 適用於: Backend + Worker + Local Development
# 最後更新: 2026-01-19 (Phase 2)
# ===========================================

# ============================================
# Backend 依賴 (Flask API)
# ============================================
Flask==2.3.0
Werkzeug==2.3.0
flask-cors==4.0.0
Flask-Limiter==3.5.0          # Phase 6: Rate Limiting

# ============================================
# Worker 依賴 (Task Processing)
# ============================================
websocket-client==1.7.0       # ComfyUI WebSocket 連接
requests==2.31.0              # HTTP Client

# ============================================
# 資料庫與快取
# ============================================
mysql-connector-python==8.2.0 # MySQL 連接
redis==5.0.1                  # Redis 客戶端 + Rate Limiter Storage

# ============================================
# Member System (Phase: Member Beta)
# ============================================
flask-login==0.6.3            # 會員登入管理
flask-bcrypt==1.0.1           # 密碼加密 (Bcrypt)
Flask-SQLAlchemy==3.1.1       # ORM 框架

# ============================================
# 工具類
# ============================================
python-dotenv==1.0.0          # 環境變數管理
colorlog==6.8.0               # Phase 2: 彩色日誌輸出
rich==13.7.0                  # Phase 6: 終端美化與 RichHandler

# ============================================
# 測試依賴 (可選，用於 tests/)
# ============================================
# playwright==1.40.0          # E2E 測試 (需要時取消註解)
# aiohttp==3.9.1              # 異步壓力測試 (需要時取消註解)

Pillow==10.1.0                # 圖片處理庫 (Worker 圖片驗證)