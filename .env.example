# ============================================
# Studio Core Environment Configuration
# ============================================
# 複製此檔案為 .env 並填入實際值
# cp .env.example .env

# ============================================
# Redis Configuration (必需)
# ============================================
# Redis 主機位址
# - 本地開發: localhost
# - Docker Compose 環境: redis
REDIS_HOST=localhost

# Redis 端口
REDIS_PORT=6379

# Redis 密碼（與 docker-compose.yml 中設定一致）
REDIS_PASSWORD=mysecret

# ============================================
# ComfyUI Configuration (必需)
# ============================================
# ComfyUI API 主機位址
# - 本地開發: localhost
# - 遠端服務器: <IP address>
COMFYUI_HOST=localhost

# ComfyUI API 端口
COMFYUI_PORT=8188

# ComfyUI WebSocket 端口（通常與 API 端口相同）
COMFYUI_WS_PORT=8188

# ============================================
# Storage Configuration (可選)
# ============================================
# 輸入檔案目錄
STORAGE_INPUT_DIR=./storage/inputs

# 輸出檔案目錄
STORAGE_OUTPUT_DIR=./storage/outputs

# 模型檔案目錄
STORAGE_MODELS_DIR=./storage/models

# ============================================
# ComfyUI Paths Configuration (重要)
# ============================================
# ComfyUI 的 input 資料夾路徑 (上傳圖片用)
# 請根據您的 ComfyUI 安裝路徑修改
COMFYUI_INPUT_DIR=D:/ComfyUI/input

# ComfyUI 的 output 資料夾路徑 (讀取輸出圖片用)
# 請根據您的 ComfyUI 安裝路徑修改
COMFYUI_OUTPUT_DIR=D:/ComfyUI/output

# ============================================
# Application Configuration (可選)
# ============================================
# Backend API 端口
BACKEND_PORT=5000

# 日志級別 (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# 任務狀態 TTL（秒）- Redis Hash 過期時間
JOB_STATUS_TTL=86400

# ============================================
# Development Configuration (開發環境)
# ============================================
# Flask Debug 模式（生產環境設為 False）
FLASK_DEBUG=True

# CORS 允許的來源（生產環境應設定具體域名）
CORS_ORIGINS=*

# ============================================
# External APIs (外部 API，Phase 2 使用)
# ============================================
# Kling Video API Key (未來使用)
# KLING_API_KEY=your_kling_api_key

# Veo3 API Key (未來使用)
# VEO3_API_KEY=your_veo3_api_key

# ============================================
# 使用說明
# ============================================
# 1. 必需配置項：REDIS_*, COMFYUI_*
# 2. 可選配置項：有默認值，可根據需要修改
# 3. 開發環境建議：保持 FLASK_DEBUG=True, LOG_LEVEL=INFO
# 4. 生產環境建議：設置 FLASK_DEBUG=False, 限制 CORS_ORIGINS
# 5. 確保 Redis 密碼與 docker-compose.yml 一致

# ============================================
# MySQL Database Configuration (Phase 3)
# ============================================
# MySQL 主機位址
# - 本地開發: localhost
# - Docker Compose 環境: mysql
DB_HOST=localhost

# MySQL 端口
# - Docker 映射端口: 3307 (避免與本地 MySQL 衝突)
# - 容器內部端口: 3306
DB_PORT=3307

# MySQL 用戶名
DB_USER=studio_user

# MySQL 密碼
DB_PASSWORD=studio_password

# MySQL 資料庫名稱
DB_NAME=studio_db

# ============================================
# 驗證配置
# ============================================
# 驗證 Redis 連接：
#   docker exec studio-redis redis-cli -a mysecret ping
#
# 驗證 ComfyUI API：
#   curl http://localhost:8188/system_stats
#
# 啟動 Backend API：
#   python backend/src/app.py
